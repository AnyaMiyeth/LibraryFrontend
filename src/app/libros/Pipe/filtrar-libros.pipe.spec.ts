import { FiltrarLibrosPipe } from './filtrar-libros.pipe';

describe('FiltrarLibrosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarLibrosPipe();
    expect(pipe).toBeTruthy();
  });
});
